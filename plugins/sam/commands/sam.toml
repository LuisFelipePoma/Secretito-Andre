description = "Show SAM quick help and next action"
prompt = "Use @sam. Show the three core phases, optional handoff commands, and current SAM status. Do not create or modify files unless the user requests init or another write action."

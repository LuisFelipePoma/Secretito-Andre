description = "Initialize three-phase SAM architecture memory"
prompt = "Use @sam init {{args}}. Initialize three-phase SAM under docs/architecture, maintain the root AGENTS.md SAM block, and use Project when NAME is absent."

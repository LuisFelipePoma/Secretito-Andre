description = "Run the next unblocked SAM step"
prompt = "Use @sam next. Determine the next allowed core or optional handoff action, report drift, and execute only an explicitly requested unblocked action."
